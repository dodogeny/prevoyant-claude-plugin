# IntelliJ IDEA
.idea/
*.iml
*.iws
*.ipr
out/

# Eclipse
.project
.classpath
.settings/
bin/
*.launch

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# Node (in case package-lock or node_modules appear)
node_modules/
package-lock.json
npm-debug.log*

# Temp / OS
Thumbs.db
*.tmp
*.log

# Polling script — credentials and runtime files (never commit these)
scripts/.jira-credentials
scripts/.jira-seen-tickets

# MCP config contains credentials (never commit)
.mcp.json
