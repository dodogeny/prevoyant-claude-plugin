{
  "name": "prevoyant-server",
  "version": "1.0.0",
  "description": "Prevoyant Server — ambient AI agent: Jira webhook receiver + scheduled poll-jira.sh trigger + stats dashboard",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "postinstall": "node scripts/ensure-qpdf.js && node scripts/ensure-lmdb.js"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "ioredis": "^5.10.1",
    "lmdb": "^3.5.4"
  },
  "engines": {
    "node": ">=18"
  }
}
